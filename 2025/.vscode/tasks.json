{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-all-days",
            "type": "shell",
            "command": "bash -c 'for dir in day*/; do day=$(echo $dir | grep -o \"[0-9]*\"); make build DAY=$day CXXFLAGS=\"-std=c++23 -Wall -Wextra -g\"; done'"
        }
    ]
}
